# Automatically convert workspace:* to actual versions when publishing
publish-branch=main
link-workspace-packages=true

# NPM registry authentication (uses NODE_AUTH_TOKEN in CI)
//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}
